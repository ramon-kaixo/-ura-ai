# Bitácora — 2026-06-15

## Objetivo
Desplegar y completar Motor de Conocimiento `ura` en GX10 ASUS y Alemania.

## Máquinas involucradas
- **GX10 ASUS** (gx10-64c3, local) — Jetson Orin aarch64, opencode activo
- **Alemania** (hetzner-escudo, 178.105.81.83) — VM Hetzner 16GB, Qdrant, Docker
- **Mac Mini** (mini-de-ramon) — offline 58d

## Resumen de acciones

### 1. Arquitectura consolidada
- Un solo comando `ura`/`ura-motor` con 20 subcomandos
- Un solo timer: `ura-pipeline.timer` cada 5 min
- 31 ficheros Python en `motor/`

### 2. Despliegue en GX10 ASUS
- Instalado como `ura-motor` (coexiste con URA legacy)
- Añadido `motor` subcommand al `/usr/local/bin/ura` existente
- Timer systemd activo
- Auto-detección físico/VM

### 3. Qdrant centralizado en ASUS
- Qdrant en ASUS rebindeado de 127.0.0.1 a 0.0.0.0
- Alemania configurada para escribir a 100.72.103.12:6333
- `_asegurar_coleccion()` crea colección al conectar (compat Qdrant 1.18.x)
- **Bug crítico encontrado y corregido:** `scan.ok` nunca se ponía `True` → `_guardar_incidente_qdrant()` nunca guardaba

### 4. Features añadidas (20 comandos)
| Comando | Descripción |
|---------|-------------|
| `pipeline` | Escanea + diagnostica + persiste (timer 5min) |
| `scan` | Solo escáner |
| `diagnose` | Solo diagnóstico |
| `status` | Estado local desde JSON |
| `check` | Preflight |
| `verify` | Verificación post-cambio |
| `history` | Incidentes desde Qdrant |
| `trend` | Historial health |
| `graph` | Gráfico ASCII health (escala 90-100) |
| `perf` | Duración por etapa del pipeline |
| `cross` | Estado GX10 + Alemania vía SSH |
| `alerta` | Alertas desde journald |
| `detect` | Anomalías vs tendencia |
| `health-check` | Autodiagnóstico (6 checks) |
| `qdrant-backup` | Exporta Qdrant a JSON |
| `notify` | Notificación desktop si health<95 |
| `learn` | Insights: crecimiento, tendencias |
| `summarise` | One-liner MOTD |
| `bench` | Benchmark (pipeline, ping, disco) |
| `calibrate` | Baseline adaptativo con trends |

### 5. Patrones de diagnóstico mejorados
- Load CPU (>2x núcleos)
- dmesg errors
- Journal corrupto
- Docker containers KO
- Flapping services
- Diff tracking
- Exit node offline

### 6. Alertas
- `ALERT_LOG` en pipeline cuando health<90 o incidentes>0
- `ura alerta` consulta journald
- `ura notify` envía notificación desktop

### 7. Tests
- 20 tests (10 legacy + 10 nuevos)
- Cobertura: detect, patterns, diff, calibration, CLI status

### 8. Fixes aplicados
- `scan.ok = True` añadido → Qdrant guarda incidentes
- Config auto-detect `/etc/ura/config.json`
- `_asegurar_coleccion` crea colección al conectar
- agent-search arrancado en Alemania
- Historial git limpiado (4 commits basura eliminados)

## Problemas conocidos
- Mac Mini offline (deploy pendiente)
- Working tree pierde .py ocasionalmente (causa no identificada)

## Archivos tocados
- `motor/cli/main.py` — 20 comandos
- `motor/core/config.py` — auto-detect config
- `motor/core/qdrant_client.py` — _asegurar_coleccion + REST fallback
- `motor/core/state.py` — contenedores_ko
- `motor/scanner/__init__.py` — _es_fisico, contenedores_ko, scan.ok, ncpu
- `motor/scanner/calibration.py` — detect() con trends
- `motor/scanner/collector_hw_asus.py` — temp_gpu fix
- `motor/diagnostico/__init__.py` — _guardar_incidente_qdrant fix
- `motor/diagnostico/pattern_matcher.py` — nuevos patrones
- `motor/pipeline/orchestrator.py` — perf tracking + trend + alertas
- `motor/guard/verifier.py` — test Ollama
- `motor/tests/test_cli.py` — 10 tests nuevos
- `motor/scripts/deploy_mac.sh` — deploy automático Mac Mini
- `motor/data/acciones_reparacion.json` — template Fase 1
- `/etc/ura/config.json` — config en GX10 y Alemania
- `/etc/systemd/system/ura-pipeline.{service,timer}` — timer en GX10
- `/usr/local/bin/ura` — motor subcommand añadido
- `/etc/update-motd.d/99-ura` — MOTD

## Servicios
- `ura-pipeline.service` — oneshot, ejecuta pipeline
- `ura-pipeline.timer` — cada 5 min, activo en ambas
- Qdrant Docker — ASUS:6333, bind 0.0.0.0

## Decisiones técnicas
- Qdrant centralizado en ASUS (no Alemania como hice inicialmente)
- REST fallback para qdrant-client (compatibilidad)
- sin `git add -A`, solo rutas específicas
- Logs a stderr, JSON a stdout

## Timers activos
- GX10: `ura-pipeline.timer` cada 5 min
- Alemania: `ura-pipeline.timer` cada 5 min

## Estado final
- GX10: health=99.3, 8 servicios, Qdrant local
- Alemania: health=99.8, 8 servicios, Qdrant→ASUS
- 20/20 tests passing
- Historial git limpio (12 commits)

## 06:11 — Usuario pide logging en tiempo real
- Cambio: AGENTS.md actualizado con Bitácora Obligatoria
- Cada mensaje/acción se loguea inmediatamente de ahora en adelante
- Formato: `HH:MM — qué se hizo`

## 06:13 — Confirmación logging en tiempo real
- Verificado: cada mensaje queda registrado al instante en bitacora/
- Commit automático tras cada entrada
- Flujo: usuario escribe → yo ejecuto → logueo → commiteo
