# NOTA: mutmut 3.7 lee la config desde pyproject.toml [tool.mutmut] (o setup.cfg).
# Este archivo mutmut.ini es formato LEGACY y ya no se usa.
# La config real (source_paths + do_not_mutate) vive en pyproject.toml.
#
# Referencia rápida de la config efectiva:
#   source_paths = motor/core/, motor/intelligence/, core/, knowledge/
#   do_not_mutate = motor/core/voice/*, knowledge/engine/extractors/audio/*
#   test_command = pytest tests/unit/ -k "not contexto_rama and not voice and not audio"
