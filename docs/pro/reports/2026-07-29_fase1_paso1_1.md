# Fase 1, Paso 1.1 — Poda de tests vacíos/muertos

**Fecha:** 2026-07-29
**Estado:** ✅ COMPLETADO

## Eliminados (3 scripts, 0 tests en recolección)
| Archivo | Líneas | Motivo |
|---------|--------|--------|
| `tests/benchmark_fase7.py` | 454 | Benchmark standalone, no se ejecuta. Sin `def test_*`. |
| `tests/e2e_fase7.py` | 237 | E2E validation script, no se ejecuta. Sin `def test_*`. |
| `tests/test_integration.py` | 162 | Integration runner con `check()` + `main()`. Sin `def test_*`. |

## Movidos a `tests/legacy/` (2 archivos, 0 tests en recolección)
| Archivo | Líneas | Motivo |
|---------|--------|--------|
| `tests/test_sda.py` | 448 | Tests reales de DebateLock, debate_engine, plan_validator. Formato `check()` legacy. Pendiente convertir a pytest. |
| `tests/test_unit.py` | 552 | Suite unitaria URA v3.0: config_manager, model_router, mantenimiento, monitor, memory_engine. Formato `check()` legacy. Pendiente convertir a pytest. |

**README** creado en `tests/legacy/README.md` documentando ambos archivos.

## Corregido (1 test trivial)
- `tests/test_assistant_auth.py::TestGitCommitTool::test_rejects_empty_message`: era `pass`, ahora ejecuta `GitCommitTool().execute(message="")` y verifica `not result.success`.

## Verificación
- Recolección pytest: **2,836 tests** (sin cambios — los archivos eliminados no contribuían tests)
- `test_assistant_auth.py`: **15 passed** (1 más que antes, el test corregido ahora ejecuta lógica real)
- Sin regresiones. Sin producción tocada.

## Próximo paso
Paso 1.2: eliminar tests infra duplicados.
