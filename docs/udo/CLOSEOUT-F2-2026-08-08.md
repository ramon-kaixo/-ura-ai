# CLOSEOUT — UDO F2: Reserva de archivos + Verificación + Modelo Dual

**Fecha:** 2026-08-08
**Tipo:** Closeout de fase (F2)
**Tag sugerido:** `v0.30.0-f2` (pendiente aprobación Ramón)
**Expedientes:** TASK-20260808-005 (implementación), TASK-20260808-006 (auditoría)

---

## 1. Alcance de F2

Mecanismo de **reserva de archivos** Web/Terminal (cortesía + auditoría), verificación
de discrepancias contra Git, y soporte operativo del **modelo dual** (Web ejecuta /
Terminal revisa) definido en el Anexo A del encargo de Ramón.

## 2. Implementado

| Elemento | Detalle |
|---|---|
| `ura-udo reserve TASK --add/--clear` | Reserva acumulativa persistente en el expediente (Git) |
| `ura-udo check [rutas...]` | Lista reservas activas; con rutas, detector de CONFLICTO (match exacto o prefijo `dir/`) |
| `ura-udo update --reserva` | Declarar/reemplazar reserva; `""` vacía |
| `commit_base` automático | Al pasar a IN_PROGRESS, o auto-relleno en tareas pre-F2 ya en IN_PROGRESS |
| `ura-udo verify` | Request + commit con [TASK-ID] + discrepancias (MODIFICADOS SIN DECLARAR / RESERVADOS NO MODIFICADOS) + registra commit en `commits:` + GIT |
| Reserva activa en REVIEW | Quien revisa no modifica la zona que revisa (principio dual) |
| Liberación automática | Al pasar a DONE/CANCELLED la reserva deja de proteger |
| `--agente_web` / `--agente_terminal` | Roles duales por tarea (ejecutor/revisor) |
| `ura-opencode` | Marca roles (WEB ejecutor / TERM revisor) al crear la tarea |
| Compatibilidad F1 | Tareas sin `reserva:`/`commit_base:` funcionan (verify tolerante, campos auto-registrados) |

## 3. Validaciones ejecutadas

| Validación | Resultado |
|---|---|
| `make validate` completo | ✅ 5251 passed, 0 failures, 38 skipped |
| Transiciones de estado (PLANNED→IN_PROGRESS→REVIEW→BLOCKED→DONE→CANCELLED, inválido rechazado) | ✅ |
| `check` sin rutas (lista activas) / con rutas libres / con conflicto | ✅ (incl. tarea en REVIEW) |
| Prefijo de directorio (`docs/udo/tasks` bloquea `docs/udo/tasks/X.md`) y no falso-positivo (`tasks2/`) | ✅ |
| Reserva acumulativa (`--add` x2) + `--clear` + liberación al DONE | ✅ |
| `commit_base` auto en tarea nueva y en tarea F1 ya IN_PROGRESS | ✅ |
| `verify` en tareas F1 (sin campos) — exit 0 | ✅ |
| `verify` registra commit en `commits:` con dedup | ✅ |
| Concurrencia: flock excluye segundo escritor; historial sin pérdidas | ✅ |
| E2E en tareas reales 005/006 (reserva real, commits, discrepancias) | ✅ |

## 4. Defectos encontrados y corregidos durante la auditoría

| # | Defecto | Corrección |
|---|---|---|
| D1 | `verify` moría (exit 1) con tareas F1 sin `reserva:`/`commit_base:` por `set -e` | `grep` tolerante (`|| true`), defaults `—` |
| D2 | `commits:` nunca se rellenaba; con dedup roto (`grep` regex trataba `[TASK-...]` como clase → "Final de rango inválido" → duplicados) | `verify` registra el commit con `grep -qF` (fixed string) |
| D3 | `--reserva ""` no vaciaba la reserva | flag `reserva_flag` permite reset a `[]` |
| D4 | `commit_base` no se auto-rellenaba en tareas F1 ya IN_PROGRESS | auto-relleno si está vacío en cualquier `update` sobre tarea activa |
| D5 | Match de conflicto `"$area"*` daba falso positivo con prefijos parciales (`tasks2`) | `== "$area" || == "$area/"*` (frontera real) |
| D6 | `update --agente_web`/`--agente_terminal` juntos: solo el último prevalecía | variables independientes |
| D7 | `check` imprimía las reservas duplicadas antes del resultado | bucle sin `echo` extra |
| D8 | Archivo suelto `9` creado por un test de flock en el repo | eliminado (test corregido, sin secuelas) |

## 5. Límites documentados (honestidad del mecanismo)

- La reserva es **cortesía + auditoría, no exclusión física**: Git sigue siendo la
  fuente de verdad y el conflicto real se detecta al commitear.
- Funciona si **ambos canales consultan** (`ura-udo check`) antes de actuar.
- `verify` lista las invasiones (no las bloquea); el flujo de commit con `[TASK-ID]`
  permite correlacionar quién tocó qué.

## 6. Trabajo pendiente de OpenCode Web (no inventado)

| Pendiente | Quién | Estado real |
|---|---|---|
| Auditoría independiente de UDO F1/F2 (expediente TASK-20260808-002) | OpenCode Web | ⏸️ Sesión `ses_01fb59...` **idle** (solo 2 mensajes, sin trabajo). No auditó nada. |
| Revisión formal del cierre de F2 (roles invertidos: Web revisor) | OpenCode Web | Pendiente — no hay evidencia de actividad |
| Uso real del mecanismo desde el canal Web (crear tareas, reservar, consultar) | OpenCode Web | Pendiente — requiere `ura-opencode` funcional |

## 7. Verificaciones que no puedo hacer desde Terminal

- **Ejecución real de OpenCode Web** sobre el repo (solo puedo consultar su sesión vía SQLite; no puedo forzar ni simular su auditoría).
- **Evaluación humana** del modelo dual (Ramón: ¿el flujo es usable?).
- **Pruebas de regresión del pipeline completo** fuera de `make validate` (E2E con servicios reales de fase 14, benchmarks — no ejecutados en esta auditoría).

## 8. Estado de la fase

**F2: DONE** (implementación + auditoría por Terminal, validación completa).
Pendiente formal: aprobación de Ramón y revisión independiente de OpenCode Web
(no bloqueante, documentado en §6).

## 9. Commits de F2

- `cc59d36d` feat(udo): [TASK-20260808-005][TERM] F2 reserva de archivos + verificación discrepancias
- (este closeout) docs(udo): [TASK-20260808-006][TERM] closeout F2 — auditoría, fixes D1-D8, modelo dual

## 10. Siguiente paso

**No abrir F3** (revisión formal con CHANGES_REQUESTED/APPROVED) hasta que:
1. Ramón apruebe este closeout.
2. OpenCode Web demuestre que puede operar el mecanismo (o se decida su sustitución).
3. Se decida el destino de `TASK-20260808-002` (auditoría Web que quedó idle).
