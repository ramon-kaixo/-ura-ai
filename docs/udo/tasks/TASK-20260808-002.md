---
id: TASK-20260808-002
fecha: 2026-08-08
solicitante: TERM
descripcion: # AUDITORÍA F1 UDO — PREPARACIÓN DE F2

Estamos cerrando UDO F1 en URA y antes de comenzar F2 quiero una auditoría técnica estricta.

## Contexto

El cierre de F1 está documentado en:

`docs/udo/CLOSEOUT-2026-08-08.md`

Estado actual:

* UDO F1 implementado.
* `scripts/pro/ura-udo` creado.
* `docs/udo/` creado.
* OpenClaw retirado del código vivo.
* `make validate`: 5251 tests passed, 38 skipped, 135 deselected, 0 failures.
* Árbol Git limpio.
* Último commit: `9c5630d4`.
* No hacer push a GitHub.
* No modificar código todavía.

## Objetivo

Quiero que audites críticamente F1 antes de continuar con F2.

No quiero una nueva implementación ni una refactorización todavía.

Quiero saber si existen problemas reales que debamos corregir antes de F2.

## 1. Revisar UDO F1

Audita:

* `scripts/pro/ura-udo`
* `docs/udo/`
* `.seq`
* `.lock`
* integración con `ura-opencode`
* cambios realizados en `AGENTS.md`
* estados de tareas
* generación de TASK-ID
* historial de tareas
* mecanismo `flock`
* relación entre tarea y Git.

Especial atención a esta cuestión:

`.seq` está versionado en Git.

Determina si dos procesos independientes (Web y Terminal) pueden provocar conflictos de Git al modificar simultáneamente `.seq`.

No des por bueno el diseño simplemente porque `flock` evita una carrera dentro de una máquina.

Si encuentras un problema, explica:

1. cómo ocurre;
2. qué riesgo tiene;
3. si afecta realmente al flujo;
4. solución mínima recomendada.

No implementes la solución todavía.

## 2. Revisar OpenCode real instalado

NO instales otro OpenCode.

NO elimines ningún ejecutable.

NO modifiques `/usr/local/bin/opencode`.

NO modifiques `/home/ramon/.opencode/bin/opencode`.

Primero determina exactamente qué son ambos.

Ejecuta únicamente consultas de solo lectura equivalentes a:

```bash
systemctl status ura-openclaw.service --no-pager -l
systemctl cat ura-openclaw.service
ls -l /usr/local/bin/opencode /home/ramon/.opencode/bin/opencode
file /usr/local/bin/opencode /home/ramon/.opencode/bin/opencode
head -n 80 /usr/local/bin/opencode
head -n 80 /home/ramon/.opencode/bin/opencode
```

Si algún archivo no existe, indícalo y continúa.

No ejecutes comandos destructivos.

No arranques ni detengas servicios.

No hagas cambios de configuración.

Quiero determinar exactamente:

* qué es `/usr/local/bin/opencode`;
* qué es `/home/ramon/.opencode/bin/opencode`;
* cuál es el wrapper;
* cuál es el CLI real;
* qué relación tienen con `ura-openclaw.service`;
* si existe realmente una instalación de OpenCode CLI funcional;
* si existe riesgo de confundir el gestor local con el OpenCode que queremos utilizar.

## 3. OpenClaw residual

Comprueba únicamente mediante lectura:

* estado de `ura-openclaw.service`;
* existencia de `/etc/systemd/system/ura-openclaw.service`;
* existencia de `/home/ramon/.openclaw/`;
* referencias restantes documentadas en el CLOSEOUT;
* dependencias reales de esas referencias.

NO elimines nada.

NO hagas `systemctl stop`.

NO hagas `disable`.

NO hagas `rm`.

NO modifiques nada.

Quiero primero saber exactamente qué queda.

## 4. Secretos

Audita la configuración de `ura-opencode` sin imprimir ningún secreto.

Comprueba:

* cómo busca `OPENCODE_WEB_PASS`;
* si existe `/etc/ura/secrets.env`;
* si el archivo existe, solamente muestra permisos, propietario y si contiene la variable, pero NO muestres su valor;
* si existen referencias al secreto dentro del repositorio.

Nunca muestres valores de contraseñas, tokens, API keys o secretos.

No escribas ningún secreto.

## 5. Excepciones OpenClaw

Analiza:

```text
tests/integration/test_openclaw.py
scripts/pro/tuneladora/snapshot.py
monitor/openclaw.py
monitor/snc.py
core/model_router/cli.py
data/openclaw_stats.json
```

Para cada uno determina:

* quién lo consume;
* si está realmente activo;
* si es histórico;
* si es código muerto;
* si eliminarlo podría cambiar comportamiento;
* si debe permanecer;
* si debe tratarse posteriormente en una fase independiente.

NO elimines ni modifiques ninguno.

## 6. Arquitectura para F2

Con toda la información anterior, evalúa si la arquitectura siguiente es segura:

```text
Ramón
  │
  ▼
TASK-ID
  │
  ├───────────────┐
  ▼               ▼
OpenCode Web   OpenCode Terminal
  │               │
  │               ├── consultas
  │               ├── revisión
  │               └── tareas independientes
  │
  └── programación
          │
          ▼
         Git
          │
          ▼
    docs/udo + historial
```

F2 debe permitir:

* Web programando;
* Terminal consultando;
* Terminal revisando;
* Terminal haciendo tareas independientes;
* ambos trabajando simultáneamente cuando no existe conflicto;
* bloqueo cuando intentan modificar la misma zona;
* trazabilidad mediante TASK-ID;
* relación con Git;
* recuperación después de cerrar una sesión.

No quiero:

* base de datos;
* servidor;
* panel;
* cola distribuida;
* Dispatcher complejo;
* nuevo sistema de tickets;
* almacenamiento completo de conversaciones;
* agentes LLM adicionales.

## 7. Regla fundamental

Git continúa siendo la fuente de verdad sobre el código.

La documentación debe explicar:

* qué se pidió;
* qué se hizo;
* quién lo hizo;
* qué se verificó;
* qué commit lo representa;
* qué queda pendiente.

Pero no queremos almacenar conversaciones completas innecesariamente.

## 8. Resultado solicitado

NO IMPLEMENTES NADA.

Devuélveme un informe con exactamente estas secciones:

1. `F1 — ESTADO REAL`
2. `PROBLEMAS ENCONTRADOS`
3. `RIESGOS DE .seq/.lock`
4. `ARQUITECTURA REAL DE OPENCODE`
5. `OPENCLAW RESIDUAL`
6. `SECRETOS`
7. `EXCEPCIONES`
8. `CAMBIOS NECESARIOS ANTES DE F2`
9. `COSAS QUE NO DEBEMOS TOCAR`
10. `RECOMENDACIÓN PARA F2`

Sé crítico.

No asumas que el diseño es correcto porque los tests pasan.

No hagas cambios.

No hagas push.

No instales nada.

No elimines nada.

No detengas servicios.

Primero diagnóstico; después decidiremos las modificaciones.
objetivo: 
estado: IN_PROGRESS
agente_web: 
agente_terminal: 
contexto: 
cambios: []
commits: []
revision: 
pendientes: []
resultado: 
historial:
  - "2026-08-08T09:31 | PLANNED | creada"
---
  - "2026-08-08T09:32 | IN_PROGRESS | PLANNED -> IN_PROGRESS — enviada por Terminal (bug grep detectado: TASK_ID multilínea)"
  - "2026-08-08T10:12 | IN_PROGRESS | reserva: [core/notifier.py]"
  - "2026-08-08T10:12 | IN_PROGRESS | reserva: []"
  - "2026-08-08T10:12 | IN_PROGRESS | reserva: [core/notifier.py]"
  - "2026-08-08T10:12 | IN_PROGRESS | reserva: []"
  - "2026-08-08T10:13 | IN_PROGRESS | reserva: [core/notifier.py]"
  - "2026-08-08T10:13 | IN_PROGRESS | reserva: []"
  - "2026-08-08T10:14 | IN_PROGRESS | reserva: [core/notifier.py]"
reserva: []
  - "2026-08-08T10:15 | IN_PROGRESS | reserva: [core/notifier.py]"
  - "2026-08-08T10:15 | IN_PROGRESS | reserva: [core/notifier.py, knowledge/engine/knowledge_engine.py]"
  - "2026-08-08T10:15 | IN_PROGRESS | reserva: []"
