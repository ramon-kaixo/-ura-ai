---
id: TASK-20260816-012
fecha: 2026-08-16
solicitante: RAMON
descripcion: F4 — Limpieza estructural (caches, config unificada, artefactos fuera del árbol)
objetivo: Dejar el repo sin artefactos/caches, una sola fuente de config, y backups/logs/reportes fuera del árbol.
instrucciones: Solo registro. La ejecución requiere análisis de plan y aprobación previa.
restricciones: No ejecutar la fase como parte de esta tarea. Trabajar en main.
estado: pendiente
canal: RAMON
prioridad: ALTA
ejecutor: TERM
revisor: WEB
revisor_gate: WEB
reserva: []
commit_base: 
contexto: |
  F4 es dependencia de F6-F10 (TASK-20260816-011). El repo acumula caches y artefactos
  en el árbol; config/ y configs/ duplican fuentes; backups, audit_reports,
  data/tuneladora_reports y logs viven dentro del repo.
cambios: []
commits: []
analisis: |
  Limpieza estructural con riesgo bajo si se respeta reversibilidad: mover, no eliminar,
  artefactos con valor (backups, reportes, logs). .gitignore debe cubrir el estado tras el movimiento.
validacion: |
  ruff check .
  git status limpio
  python3 -m json.tool docs/udo/coordination.json
verificar: 
requisitos: Ninguno (F4 es prerequisito de F6-F10).
resultado: 
revision: 
historial:
  - "2026-08-16 | pendiente | registrada en cola pendientes por WEB"
---

# TASK-20260816-012 — F4: Limpieza estructural

**Prioridad**: ALTA · **Ejecutor**: TERM · **Revisor**: WEB

## Alcance

1. Eliminar caches y artefactos del repo: `.venv`, `__pycache__`, `.pytest_cache`,
   `.mypy_cache`, `.ruff_cache`, `.hypothesis`, `node_modules`, `.sandbox_packages`.
2. Unificar `config/` y `configs/` en una sola fuente.
3. Mover fuera del árbol: `backups/`, `audit_reports/`, `data/tuneladora_reports/`, `logs/`.
4. Actualizar `.gitignore` completo acorde al estado final.

## Gates

- `ruff check .` — 0 errores.
- `git status` limpio (sin artefactos trackeados).
- `python3 -m json.tool docs/udo/coordination.json` — JSON válido.

## Criterio de cierre

Repo sin caches/artefactos en el árbol, una sola fuente de config, backups/reportes/logs
fuera del árbol con ruta documentada, `.gitignore` cubriendo el nuevo estado, gates verdes
y revisión WEB aprobada.
## Trazabilidad de ejecución (TERM)

- **Rama**: `ia/TASK-20260816-012` (desde main).
- **Unificación config**: `configs/ia_committee_config.json` → `config/` (nueva fuente única;
  `scripts/pro/auditoria_comite.sh` actualizado). El archivo físico original en `configs/`
  tiene **chattr +i (immutable)** — sacado del índice con `git rm --cached`, pero el borrado
  físico requiere `sudo chattr -i <archivo> && rm` en el host (pendiente humano). Ignorado
  vía `.gitignore` (`configs/`).
- **Artefactos fuera del árbol**: backups/, audit_reports/, logs/, data/tuneladora_reports/
  movidos a `/var/tmp/ura_artifacts/` (el host tiene la raíz RO en este namespace; destino
  canónico `/opt/ura/` pendiente de remount rw).
- **Caches eliminados**: .venv (696M), .mypy_cache (135M), .sandbox_packages (86M),
  .hypothesis (9.7M), .ruff_cache (3.8M), .pytest_cache (888K), __pycache__ (44K).
- **.gitignore**: añadidos `.pytest_cache/`, `configs/`, `/data/tuneladora_reports`.
