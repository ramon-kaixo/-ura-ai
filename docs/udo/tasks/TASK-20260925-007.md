---
title: SPRINT 0 - Pipeline CI Completa
task_id: TASK-20260925-007
status: PLANNED
created_by: TERM (build agent)
created_at: 2026-09-25T07:45:00Z
reserva:
  - .github/workflows/
  - pyproject.toml
  - scripts/pro/ura-udo
  - docs/udo/coordination.json

---

# SPRINT 0 - Pipeline CI Completa (v6.1)

## Objetivo 
Configurar pipeline completa de CI con validación del código y tests en < 10 minutos para flujo ágil sin timeout shell.

## Subtareas
- [ ] P0.1 Validar release.yml + ci.yml + tests.yml funcionan localmente (sin timeout)
- [ ] P0.2 Crear script segmentador de tests con marcadores pytest (`@pytest.mark.slow`, `@pytest.mark.integration`)
- [ ] P0.3 Integración testing: pytest + coverage + mypy en pipeline CI
- [ ] P0.4 Documentar solución test flaky resuelto (no regresión)

## Objetivo Final de Sprint 0  
✅ Pipeline CI completo ejecutable localmente sin timeout shell tool

## Gate de Validación 
```bash
# Test final:
ruff check . && mypy --no-incremental core motor shared --exclude "motor/tests" && pytest -k "not slow" --maxfail=1 -q
```

