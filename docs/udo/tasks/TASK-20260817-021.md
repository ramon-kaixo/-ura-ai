# TASK-20260817-021 — Clase Diagnostico duplicada (TypeError garantizado en producción)

- **Fecha**: 2026-08-17
- **Prioridad**: CRÍTICA (P0)
- **Ejecutor**: TERM
- **Revisor**: WEB
estado: aprobada
- **Origen**: hallazgo del agente Mac (integración 2026-08-17, hallazgos-fondo.md)

## Hallazgo

**Dos clases `Diagnostico` divergentes en `motor/diagnostico/`:**

| Archivo | Línea | Instancia CircuitBreaker | Llamada buscar_patrones | Estado |
|---|---|---|---|---|
| `motor/diagnostico/__init__.py` | 22 | `CircuitBreaker(qdrant)` — cliente real ✅ | **4 args** (scan, qdrant, cb, config) → **TypeError garantizado** | ❌ PRODUCCIÓN ROTA |
| `motor/diagnostico/diagnostico.py` | 27 | `CircuitBreaker("qdrant")` — **string** → `AttributeError: 'str' no tiene .health()` | 3 args (scan, qdrant, config) — correcto ✅ | ⚠️ Defecto oculto (enmascarado por tests con monkeypatch) |

**Firma real**: `motor/diagnostico/pattern_matcher.py:6` → `buscar_patrones(scan, qdrant, config) -> tuple` (3 args).

**Importadores** (verificado en ASUS):
- `motor/pipeline/orchestrator.py:10` → `from motor.diagnostico import Diagnostico` (clase ROTA de `__init__.py`)
- `motor/cli/cmd_pipeline.py:9` → `from motor.diagnostico import Diagnostico` (idem)
- `tests/unit/test_motor_diagnostico.py:11` → `from motor.diagnostico.diagnostico import Diagnostico` (la otra — por eso los tests NO detectan el TypeError de producción)

**Impacto**: `Orchestrator.run()` captura la excepción y deja `result.ok=False` — el pipeline de diagnóstico
de producción está roto y degrada en silencio. No cubierto por tests.

## Plan de consolidación (propuesto — NO ejecutado)

1. **Unificar en una sola clase**: mantener la implementación completa (la de `diagnostico.py` es la más madura:
   executor inyectable, try/except en Qdrant, guardia cb.operacional) pero **corregir sus 2 defectos**:
   - `CircuitBreaker("qdrant")` string → `CircuitBreaker(qdrant)` (cliente real).
   - Mantener llamada a `buscar_patrones(scan, self.qdrant, self.config)` (3 args, correcta).
2. **`__init__.py` deja de definir la clase** y pasa a re-exportar: `from motor.diagnostico.diagnostico import Diagnostico` — así `from motor.diagnostico import Diagnostico` (orchestrator, cmd_pipeline) sigue funcionando sin cambiar sus imports (backward-compatible).
3. **Actualizar tests** `test_motor_diagnostico.py`: añadir caso sin monkeypatch del CircuitBreaker y caso `run()` con scan.ok=True para cubrir el pipeline real (TypeError).
4. **Verificación**: `pytest tests/unit/test_motor_diagnostico.py` + smoke `python3 -m motor.cli.cmd_pipeline` sin TypeError + ruff.
5. **Reversibilidad**: un solo commit; si algo falla → `git revert`.

## Historial
- 2026-08-17 TERM: diagnóstico completo (rutas, líneas, diferencias, importadores) — SIN modificar código. Registrada en coordination.json (pendientes).
commits: [4e93c1ee fix(motor): [TASK-20260817-021][TERM] consolidar clase Diagnostico en diagnostico.py]
