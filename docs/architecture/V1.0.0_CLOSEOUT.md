# Closeout v1.0.0

| Campo | Valor |
|-------|-------|
| Versión | v1.0.0 |
| Commit | `7707625` |
| Fecha | 2026-07-20 |
| Rama | `main` |
| Tag | `v1.0.0` (firmado, forzado sobre tag anterior) |

## Auditoría Reproducible

`bash scripts/audit_reproducible.sh` — **12/12 checks, exit 0.**

| # | Check | Resultado |
|---|-------|-----------|
| 1 | Compilación desde cero (git clone + py_compile) | ✅ PASS |
| 2 | Tests (pytest) | ✅ PASS (122 passed, 0 failed) |
| 3 | Ruff | ✅ PASS (0 errores) |
| 4 | Mypy (assistant) | ✅ PASS (0 errores) |
| 5 | Bandit (motor/assistant/) | ✅ PASS (0 Medium/High) |
| 6 | Import check | ✅ PASS |
| 7 | Dependencias (requirements.txt) | ✅ PASS |
| 8 | ADRs (37) | ✅ PASS (0 Draft) |
| 9 | Código muerto | ✅ PASS (legacy documentado) |
| 10 | Nombres duplicados | ✅ PASS (intencional, namespaces separados) |
| 11 | Versión (código vs tag) | ✅ PASS (1.0.0 = v1.0.0) |
| 12 | Bandit (motor/ completo) | ✅ PASS (0 Medium/High tras fix 8 issues) |

## Estado del Repositorio

- **`git status`**: limpio (0 cambios sin commitear)
- **`main` local** = `origin/main` = `7707625`
- **Tag `v1.0.0`** en `origin`: `7707625`

## Limitaciones Conocidas (Externas al Código)

| Limitación | Detalle |
|------------|---------|
| Rootfs RO | `/` montado `ro,errors=remount-ro` — impide `pip install`, `sudo`, modificación del sistema |
| `no new privileges` | Flag del sistema impide `sudo` (F14-F01) |
| Contenedor sandbox | RO filesystem + sin git + conflicto ARM native extensions — no usable para audit CI |
| PEP 668 | `pip install` bloqueado en el host por `externally-managed-environment` |
| Prueba en contenedor | No es viable sin un contenedor con permisos de escritura |

El audit está diseñado para ejecutarse en **CI/CD (GitHub Actions)** o en un entorno con permisos de escritura. Las limitaciones son del sistema GX10, no del proyecto.

## Historial

- `v0.14.8-b5` → ... → `v1.0.0-rc` (2026-07-16) → audit profundo → `v1.0.0` (2026-07-20)
- Ramas cerradas: `plan-refinado` (fusionada en `main`)
- No hay F30+ definida. Trabajo futuro en rama `develop` o `v1.1.0`.

## Congelación

A partir de este commit, `main` y `v1.0.0` quedan congelados. Cualquier cambio futuro debe hacerse en una rama nueva (`develop`, `v1.1.0`, etc.) y pasar por revisión antes de mergear a `main`.
