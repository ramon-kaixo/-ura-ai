{
  "worker": {
    "descripcion": "Nodo worker estandar para buzos y procesamiento",
    "pasos": [
      {"comando": "mkdir -p /opt/ura", "descripcion": "Crear directorio base"},
      {"comando": "apt update && apt install -y python3 python3-pip curl jq", "descripcion": "Instalar dependencias base", "verificacion": "python3 --version"},
      {"comando": "curl -fsSL https://tailscale.com/install.sh | sh", "descripcion": "Instalar Tailscale", "verificacion": "tailscale version"},
      {"comando": "pip3 install requests paramiko pandas", "descripcion": "Instalar librerias Python"}
    ]
  },
  "worker_arm64": {
    "descripcion": "Nodo worker ARM64",
    "pasos": [
      {"comando": "mkdir -p /opt/ura", "descripcion": "Crear directorio base"},
      {"comando": "apt update && apt install -y python3 python3-pip curl jq", "descripcion": "Instalar dependencias base", "verificacion": "python3 --version"},
      {"comando": "curl -fsSL https://tailscale.com/install.sh | sh", "descripcion": "Instalar Tailscale", "verificacion": "tailscale version"}
    ]
  },
  "worker_amd64": {
    "descripcion": "Nodo worker AMD64 con Docker",
    "pasos": [
      {"comando": "mkdir -p /opt/ura", "descripcion": "Crear directorio base"},
      {"comando": "apt update && apt install -y python3 python3-pip docker.io curl jq", "descripcion": "Instalar Python + Docker", "verificacion": "docker --version"},
      {"comando": "curl -fsSL https://tailscale.com/install.sh | sh", "descripcion": "Instalar Tailscale", "verificacion": "tailscale version"}
    ]
  },
  "tpv-server": {
    "descripcion": "Servidor TPV para gestion de ventas",
    "pasos": [
      {"comando": "mkdir -p /opt/ura/data", "descripcion": "Crear directorios"},
      {"comando": "apt update && apt install -y python3 python3-pip sqlite3", "descripcion": "Instalar dependencias", "verificacion": "python3 --version"},
      {"comando": "pip3 install pandas requests", "descripcion": "Instalar librerias"}
    ]
  },
  "camaras": {
    "descripcion": "Nodo para procesamiento de camaras y Frigate",
    "pasos": [
      {"comando": "mkdir -p /opt/ura/frigate", "descripcion": "Crear directorios"},
      {"comando": "apt update && apt install -y docker.io docker-compose", "descripcion": "Instalar Docker", "verificacion": "docker --version"},
      {"comando": "systemctl enable docker && systemctl start docker", "descripcion": "Iniciar Docker"}
    ]
  },
  "master": {
    "descripcion": "Nodo maestro con Ollama y servicios completos",
    "pasos": [
      {"comando": "mkdir -p /opt/ura", "descripcion": "Crear directorio base"},
      {"comando": "curl -fsSL https://ollama.com/install.sh | sh", "descripcion": "Instalar Ollama", "verificacion": "ollama --version"},
      {"comando": "ollama pull qwen2.5-coder:14b", "descripcion": "Descargar modelo"},
      {"comando": "pip3 install requests paramiko pandas chromadb", "descripcion": "Instalar librerias"}
    ]
  },
  "storage": {
    "descripcion": "Nodo de almacenamiento para backups y datos",
    "pasos": [
      {"comando": "mkdir -p /opt/ura/backups", "descripcion": "Crear directorio backups"},
      {"comando": "apt update && apt install -y rsync nfs-common", "descripcion": "Instalar herramientas sync"}
    ]
  }
}
