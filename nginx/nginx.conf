events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location / {
            return 200 "URA API running in headless mode\n";
            add_header Content-Type text/plain;
        }
    }
}
