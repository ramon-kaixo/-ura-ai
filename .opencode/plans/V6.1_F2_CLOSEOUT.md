# F2 Closeout — Assistant Context Integration

**Estado:**
- Original: perdido (untracked, borrado por git clean -fd)
- Fuente de reconstrucción: código (commit c3ac1f9 en v0.29.1), diff entre 16b34cf y v0.29.1 en motor/assistant/
- Nivel de confianza:
  - Código modificado: Alto (git diff verificable con --stat)
  - Líneas cambiadas: Alto (64 insertions, 22 deletions en 4 archivos)
  - Decisiones documentadas: Alto (citadas del código)
  - Nombres de archivos y clases: Alto (verificados en git show)
  - Metadata editorial (riesgos sin verificar, fechas): Estimada

---

## Implementación

### Archivos modificados (4 files, +64−22)

| Archivo | Cambio | Líneas |
|---------|--------|--------|
| `motor/assistant/llm_bridge.py` | `_local_generate()` usa `motor.core.llm.generate()` en vez de OllamaProvider | +5−3 |
| `motor/assistant/conversation.py` | `process_user_message()` retorna `rag_context` + `semantic_context` | +22−1 |
| `motor/assistant/rag.py` | `SQLiteMemoryStore` con db_path real, `retrieve_sync()`, `is_available()` | +29−7 |
| `motor/assistant/api.py` | `_add_context_sections()` injected en _build_system_prompt | +30−12 |

### Detalle de cambios por archivo

#### motor/assistant/llm_bridge.py 

`_local_generate()` (línea 182):
```python
def _local_generate(self, messages, model_key):
    from motor.core.llm import generate as core_generate
    prompt = self._messages_to_prompt(messages)
    return core_generate(prompt, model=model_key)
```

- `OllamaProvider` eliminado (0 referencias restantes en motor/assistant/)
- Delegación a `motor.core.llm.generate()` — el router elige proveedor según capacidad

#### motor/assistant/conversation.py 

`process_user_message()` (línea 176):
```python
rag_text = self._rag.retrieve_sync(user_message) if self._rag.is_available() else ""
semantic_facts = self._query_semantic_facts(user_message)
# ...
return {
    "rag_context": rag_text,
    "semantic_context": semantic_facts,
}
```

Nuevo método `_query_semantic_facts()` (línea 305):
```python
@staticmethod
def _query_semantic_facts(query, max_facts=5) -> str:
    from motor.intelligence.memory.semantic import SemanticMemoryStore
    store = SemanticMemoryStore()
    results = store.search(query, limit=max_facts)
    return _format_facts(results)
```

#### motor/assistant/rag.py

`RAGContext` refactorizado con:
- `_get_ke_db_path()` — detecta knowledge.db en `~/URA/ura_ia_1972/knowledge/`
- `_check_available()` — usa `SQLiteMemoryStore` (antes `MemoryStore` que no existía)
- `is_available()` — nuevo método público
- `retrieve_sync()` — nuevo método sincrónico (antes solo `async retrieve()`)

#### motor/assistant/api.py

`_add_context_sections()` (línea 235) añade al system prompt:
```python
if analysis.get("rag_context"):
    system_prompt += f"\n[Contexto documental: {analysis['rag_context']}]"
if analysis.get("semantic_context"):
    system_prompt += f"\n{analysis['semantic_context']}"
```

### E2E Test

```python
# Verifica 9 condiciones (asserts) en 5 grupos:
# Grupo 1 (2 asserts): LLMBridge importa core_generate, no OllamaProvider
# Grupo 2 (3 asserts): process_user_message retorna semantic_context + rag_context
# Grupo 3 (1 assert): _query_semantic_facts() existe y retorna str
# Grupo 4 (2 asserts): _add_context_sections incluye rag_context + semantic_context
# Grupo 5 (1 assert): RAGContext tiene retrieve_sync()
```

Archivo: `/tmp/test_f2_e2e.py` (53 lines, 9 asserts)

Nota: La lista original (6 condiciones) tenia 2 errores:
- Condicion 5 (RAGContext.is_available()) solo se imprime, no se afirma
- Condicion 6 (retrieve_sync retorna str) el test verifica hasattr, no str
- Omitia el Grupo 4 (_add_context_sections) que tiene 2 asserts
- El test real tiene 5 grupos, no 6

### Integración Index

| Sistema | Consumidores post-F2 | Código ejecutado |
|---------|---------------------|------------------|
| motor.assistant | 2 contextos (RAG + Semantic) + LLMBridge refactor | `process_user_message()` → `retrieve_sync()` + `_query_semantic_facts()` |
| Motor Core LLM | +1 consumidor (assistant via bridge) | `motor.core.llm.generate()` |
| KE MemoryStore | +1 consumidor (assistant rag.py) | `SQLiteMemoryStore.search()` |
| SemanticMemoryStore | +1 consumidor (assistant conversation.py) | `SemanticMemoryStore.search()` |

### Verification

| Check | Resultado | Comando |
|-------|-----------|---------|
| Ruff assistant | 0 errores | `ruff check motor/assistant/` ✅ |
| Ruff F1+F2 | 1 error (pre-existente en fusion) | `ruff check knowledge/engine/ motor/core/fusion/ motor/assistant/` ✅ |
| Compilación | py_compile OK | `python3 -m py_compile motor/assistant/*.py` ✅ |
| E2E test | 9/9 asserts (5 grupos) | `python3 /tmp/test_f2_e2e.py` ✅ |
| pytest | 0 regresiones | `pytest -q motor/tests/` ✅ |

### Riesgos documentados

| ID | Riesgo | Severidad | Estado |
|----|--------|-----------|--------|
| R1 | `rag.py:_get_ke_db_path()` hardcodea ruta `~/URA/ura_ia_1972/knowledge/knowledge.db` | Media — no funciona si KE DB está en otra ruta | PENDIENTE |
| R2 | `HistoricalMemoryAdapter` existe en SemanticMemoryStore pero no tiene consumidores en producción | Baja — código muerto no dañino | PENDIENTE |
| R3 | `motor/assistant/` no tiene métricas, health, tracing ni correlation_id | Alta — F3 debe resolverlo | PENDIENTE (F3) |
