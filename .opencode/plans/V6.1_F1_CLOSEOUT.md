# F1 Closeout — KE → Fusion Integration

**Estado:**
- Original: perdido (untracked, borrado por git clean -fd)
- Fuente de reconstrucción: código (commit 5089a50 en v0.29.1), diff entre 16b34cf y v0.29.1 en knowledge/engine/ + motor/core/fusion/ + motor/__init__.py
- Nivel de confianza:
  - Código modificado: Alto (git diff verificable con --stat)
  - Líneas cambiadas: Alto (246 insertions, 11 deletions)
  - Decisiones documentadas: Alto (citadas del código)
  - Metadata editorial (estimaciones de esfuerzo, fechas de revisión): Estimada

---

## Implementación

### Archivos modificados (8 files, +246−11)

| Archivo | Cambio | Líneas |
|---------|--------|--------|
| `knowledge/engine/subscribers.py` | `_make_fusion_subscriber` — thin handler (~15 lines) | +44 |
| `knowledge/engine/orchestrator.py` | `compile_result_to_claims()` — KE→KnowledgeClaim conversion | +55 |
| `knowledge/engine/metrics.py` | `record_fusion()` — telemetry counters | +36 |
| `knowledge/engine/api.py` | Register subscriber in API lifespan | +4 |
| `knowledge/engine/cli/main.py` | `_init_bus()` — register subscriber in CLI entry | +15 |
| `knowledge/engine/collector.py` | Lazy init guard + import fix | +23 |
| `motor/core/fusion/__init__.py` | `run_fusion_on_claims()` — entity resolution → merge → SemanticMemoryStore | +66 |
| `motor/__init__.py` | Editable install guard (path conflict resolution) | +14 |

### Handler fusion (`subscribers.py:176-197`)

```python
def handler(event: CompileCompleted) -> None:
    t0 = _time.monotonic()
    try:
        claims = compile_result_to_claims(db_path)
        if claims:
            n = run_fusion_on_claims(claims, correlation_id=event.correlation_id)
            dur = _time.monotonic() - t0
            record_fusion(claims=len(claims), facts=n, duration=dur)
    except Exception as exc:
        record_fusion(status="error", duration=(_time.monotonic() - t0))
        log.warning("Fusion handler failed: %s", exc)
```

### 5 handlers para CompileCompleted

| Handler | Propósito | Archivo |
|---------|-----------|---------|
| `_make_compile_archive_handler` | Encola archive job post-compile | subscribers.py:54 |
| `_make_compile_audit_handler` | Registra auditoría post-compile | subscribers.py:68 |
| `_make_compile_metrics_handler` | Métricas post-compile | subscribers.py:82 |
| `_make_lineage_subscriber` | OpenLineage events | subscribers.py:95 |
| `_make_fusion_subscriber` | Fusión de conocimiento (F1) | subscribers.py:167 |

### Idempotencia

- `subscribe_all()` protegido por `_SUBSCRIBED` flag global (`subscribers.py:17-20`)
- `collector.py` inicialización lazy via `_BUS_INITIALIZED` — no side effect al importar (`collector.py:7-8`)

## Arquitectura (ARQ-800)

| Dimensión | Source Owner | Target Owner | Conversion Location |
|-----------|-------------|--------------|-------------------|
| Document → KnowledgeClaim | KE (`orchestrator.py`) | KE | `compile_result_to_claims()` |
| KnowledgeClaim → SemanticFact | Fusión (`fusion/bridge.py`) | Fusión | `knowledge_fact_to_semantic_fact()` |
| SemanticFact → MemoryEntry | Fusión (`__init__.py`) | Fusión | `run_fusion_on_claims()` |

## Integración Index

| Sistema | Consumidores post-F1 | Código ejecutado |
|---------|---------------------|------------------|
| KE EventBus | 5 handlers (archive, audit, metrics, lineage, fusion) | `subscribe_all()` en API lifespan + CLI |
| Fusion (motor/core/fusion/) | Llamado desde subscriber | `run_fusion_on_claims()` |
| SemanticMemory | Poblado por Fusion | `SemanticMemoryStore(search=...)` |
| KE Metrics | +1 métrica: record_fusion | 3 counters + 1 histogram |
| CLI | +1 entry: _init_bus() en main | `subscribe_all()` en CLI startup |
| API | +1 entry: subscribe en lifespan | `subscribe_all()` en API startup |

## Verification

| Check | Criterio | Comando |
|-------|----------|---------|
| Compilación | py_compile 0 errores | `python3 -m py_compile knowledge/engine/subscribers.py` ✅ |
| Ruff | 0 errores nuevos | `ruff check knowledge/engine/ motor/core/fusion/` — 1 error (pre-existente en fusion) |
| pytest | 0 regresiones | `pytest -q -k "knowledge"` ✅ |
| Tree | F1 commit no cambia árbol no relacionado | `git diff 16b34cf..5089a50 --stat` — solo 8 files tocados ✅ |
