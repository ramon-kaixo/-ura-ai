# /etc/ura/fix-path.conf.example
# URA — Environment overrides for ura-contraste service
# Copy to /etc/ura/fix-path.conf and adjust paths for your deployment.
URA_HOME=/path/to/ura
PATH=/usr/local/bin:/usr/bin:/bin:/home/user/.local/bin
PYTHONPATH=/path/to/agents
