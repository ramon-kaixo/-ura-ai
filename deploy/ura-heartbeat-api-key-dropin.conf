[Service]
# Inyectar URA_API_KEY como env var (defensa en profundidad)
# get_secret() ya lee /etc/ura/secrets.env, pero tenerlo como env
# evita accesos a disco en cada health check
EnvironmentFile=/etc/ura/secrets.env
