# Drop-in para ura-heartbeat.service (WEB 2026-08-18)
# El unit original tiene ProtectHome=read-only -> el heartbeat NO puede escribir
# data/auto_dumps (auto-recuperacion) aunque el rootfs sea rw. Se relaja SOLO
# ProtectHome (el resto del sandboxing se mantiene).
[Service]
ProtectHome=no
