FROM python:3.12-slim
RUN apt-get update && apt-get install -y curl docker.io
COPY . /workspace
WORKDIR /workspace
RUN python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
CMD ["bash", "cloud/entrypoint.sh"]
